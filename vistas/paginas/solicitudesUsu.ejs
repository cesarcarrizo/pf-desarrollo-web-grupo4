<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('parciales/head') %>
    <title>TECH TEAM - PROYECTOS - Solicitudes</title>
    <style>
      body {
        background: url("/static/labe.jpg") no-repeat center center fixed;
        -webkit-background-size: cover;
        -moz-background-size: cover;
        background-size: cover;
        -o-background-size: cover;
      }
    </style>
  </head>
  <body>
    <%- include('parciales/encabezadoProf') %>
    <!-- BODY -->
    <div class="container">
      <div class="jumbotron bg-light">
        <div class="back"></div>
        <h1>TECH TEAM</h1>
        <p>Solicitudes pendientes:</p>

        <div class="jumbotron bg-dark text-white">
          <form method="POST" role="form">
            <div class="form-group">
              <label>Revise la lista de proyectos pendientes:</label>
              <select class="form-control" name="seleccion">
                <% peticiones.forEach((peticion)=>{ %>
                <option
                  ><%= peticion.ced_cli_pk %> - Cliente: <%= peticion.nom_cli %>
                  <%=peticion.ape_cli%> - Área de especialización: <%
                  peticion.nom_esp %>(Encargado de área: <%=peticion.nom_pro%>
                  <%=peticion.ape_pro%>) - Solicitud hecha el
                  <%=peticion.fecha_solicitud_proy %> <% }); %>
                </option></select
              >
            </div>

            <div class="form-group">
              <input
                type="submit"
                name="contratado"
                value="Aceptar"
                class="btn btn-outline-success"
              />
              <input
                type="submit"
                name="rechazado"
                value="Rechazar"
                class="btn btn-outline-danger"
              />
            </div>
          </form>
        </div>
      </div>
    </div>

    <!-- FIN -->
    <%- include('parciales/navProf') %> <%- include('parciales/scripts') %>
  </body>
</html>
